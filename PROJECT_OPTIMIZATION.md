# 项目工程优化 Backlog

> 本文件是工程执行清单，不是项目目标或策略准入规则。  
> 核心宗旨、研究优先级、策略状态、生产准入和资金风险规则，以 [`PROJECT.md`](PROJECT.md) 为唯一依据。

## 1. 优化工作的唯一目的

工程优化只能服务于以下至少一项：

```text
账户与凭据安全；
数据、策略、账本和订单正确性；
真实交易执行与风险状态生成；
生产可观测性、故障告警、回滚与恢复；
降低对稳定收益和风险控制有实质影响的延迟或成本。
```

纯性能、架构美化、功能堆叠或重构，不得抢占影响策略正确性、生产安全和风险控制的优先级。

---

## 2. 优先级规则

### P0：生产阻断项

未完成以下事项前，系统不得进入 LIMITED_LIVE_READY：

```text
1. 凭据、密钥、数据库密码和交易 Token 不得写入仓库或日志；
2. 所有交易执行 API、管理入口和自动下单路径必须鉴权；
3. 持仓、现金、订单与净值对账必须具备事务性或原子性；
4. 数据字段、复权语义、交易日和价格口径必须可验证；
5. 风险状态、订单执行、未成交处理和停机逻辑必须可审计；
6. 数据异常、账本不一致、风控状态生成失败必须触发停止新增订单；
7. 核心策略、风险预算、订单与账本路径必须具备自动化测试。
```

### P1：生产必要能力

```text
结构化日志与分级持久化；
失败告警与通知闭环；
端到端 pipeline smoke test；
CI 检查；
数据快照、配置 SHA、代码 SHA 与结果报告绑定；
流动性、成本、滑点、部分成交与未成交处理的统一执行模型；
运行指标：任务耗时、数据完整率、订单成功率、风险状态分布、实际暴露偏差。
```

### P2：在 P0/P1 后评估

```text
评分链路向量化和 SQL 优化；
连接池与查询优化；
模块边界整理、共享基础设施层；
调度器收敛；
通知模块拆分；
历史死代码清理；
开发体验与包管理改进。
```

### P3：非当前优先级

```text
多频率回测扩展；
多资产与多策略组合框架；
界面、报表和非生产功能增强；
不直接影响稳定收益、风险控制或生产安全的重构。
```

---

## 3. 工程变更准入

每个优化任务在实施前必须标记：

```text
关联目标：安全 / 正确性 / 执行 / 风控 / 可观测性 / 性能
影响模块：
是否改变策略逻辑：是 / 否
是否改变历史回测结果：是 / 否
验证方式：单元测试 / 集成测试 / 回放 / 影子运行
回滚方式：
```

凡是可能改变策略信号、仓位、订单、成本、净值或风险状态的工程变更，必须：

```text
独立提交；
提供变更前后结果差异；
提供账本对账；
提供回滚方式；
不得与参数优化或策略研究混在同一提交中。
```

---

## 4. 与 B2_SCALE40 的直接生产依赖

B2 当前处于 `PRIMARY_RISK_BUDGET_CANDIDATE_PENDING_OOS`。在进入影子或生产运行前，工程侧至少要具备：

```text
T 日信号与 T+1 开盘执行映射；
风险状态生成失败保护；
目标仓位与实际仓位逐日对账；
订单、持仓、现金和净值四账一致性检查；
涨跌停、停牌、整手、最小成交额、部分成交和未成交处理；
真实成本与滑点记录；
风险状态、配置、数据快照和代码版本可回放；
数据异常、账本异常和成本异常告警；
影子运行报告自动生成。
```

---

## 5. 历史优化审查的处理方式

此前工程审查中列出的安全、正确性、性能、架构、可观测性和测试事项，均应按本文件重新分类和排期。

```text
影响安全、账本、交易、数据语义或风险控制：P0
影响生产可观测性、告警、测试与回放：P1
纯性能或架构改善：P2/P3
```

历史清单保留在 Git 历史中，仅作为追溯材料；它不得改变 `PROJECT.md` 已冻结的策略治理和生产准入标准。

---

## 6. 禁止事项

```text
用重构替代策略有效性验证；
因实现困难绕过成交、账本或风控约束；
为追求性能删除审计字段或降低日志质量；
将生产修复与策略参数调整混合提交；
未完成 P0 项目即进入有限实盘；
将工程复杂度误认为策略竞争力。
```
