# Alpha v3.4 Replay Audit 升级记录

## 目标

根据 v3.3 第二轮独立复评，把验证链从“证明框架”推进到“证据消费与重放审计”。
本轮不增加新 Alpha、不修改生产路由、不启用 Canary，也不补造三基准、PIT、
因子或真实 Shadow 数据。

## 实现

- 默认档案升级为 `alpha_v3_4 / alpha_v3_4_evidence_v1`。
- 新增 `factor_compute_lineage_report.json`：逐因子绑定公式版本、代码文件 SHA、
  配置文件 SHA、输入快照 SHA、带时区生成时间和 `factor_pipeline_hash`。
- 新增 `research_replay_report.json`：精确比较 release、策略、档案、代码、配置、
  输入、NAV、交易、归因和风险 SHA。
- 新增 `failure_injection_report.json`：确定性注入 pipeline hash 篡改、无时区、
  因子晚于信号时间和基准末日错位，要求全部被失败关闭。
- 新增 `execution_stress_report.json`：模拟涨停买不到、跌停卖不出、延迟成交和
  2 倍滑点；结果不能替代真实 Shadow。

## 验证

- 相关回归：`45 passed`。
- Python 3.11 非集成完整套件：`1511 passed, 11 skipped, 7 deselected`。
- reference 与 replay 的重放指纹相同：
  `ef4d5d2c889d08599d32c35ee2b020dec7d8e51ba364144a388191b47860d101`。
- replay 证据包重复执行 SHA：
  `49c02bb7723c85f342f3a4bf7c4652dbce31554d62ea9129c785af8347463e8c`。

## 实际结论

`research_replay` 与四项故障注入通过。正式因子计算 manifest 缺失，交易明细缺少
`limit_status` 和 `fill_status`，因此计算血缘与执行压力仍为 `BLOCKED`。历史指标
保持年化 20.37%、最大回撤 -25.52%、Sharpe 0.82；总状态继续
`BLOCKED / NO_SCALE`，允许新增资金 0 元。

正式输出：

- `exports/alpha_v3_validation/20260730_alpha_v3_4_reference/`
- `exports/alpha_v3_validation/20260730_alpha_v3_4_replay/`

