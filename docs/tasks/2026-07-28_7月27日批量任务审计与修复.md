# 2026-07-27 批量任务审计与修复

## 结论

7 月 27 日生产批量任务有 1 个真实失败和 2 个依赖阻断：

- `trusted_strategy_candidates` 生成的生产配置 SHA 已升级为 64 位，但
  `ads_production_risk_decisions.config_sha` 仍为 `VARCHAR(32)`，写入风险闸审计时失败。
- `trusted_strategy_shadow_monitor` 与 `trusted_strategy_performance_review`
  因候选任务失败被依赖门禁阻断。
- 其他日批任务均有成功队列记录或严格业务产物证据；周度图片清理因当天不是周五而合法跳过。

## 修复

- 新建风险闸表时将 `config_sha` 定义为 `VARCHAR(64)`。
- 对已存在的旧表，在写入前读取实际列宽；不足 64 位时自动执行兼容升级。
- 日终巡检增加影子盘日级结果与收益复盘文件的恢复证据识别，保留原失败/阻断记录，
  不篡改历史队列状态。

## 补跑结果

- 候选导出：5 条生产候选、5 条每日信号快照、1 条风险闸记录；未生成订单、未补发历史通知。
- 影子盘：3 条订单均可成交，其中 2 条有大滑点警告；平均滑点 610.18 bps，
  验收状态为 `fail / reduce_position`，风控状态未绕过。
- 收益复盘：成功生成替代诊断报告
  `exports/production_strategy_reviews/20260728_115932_20260727/`。
- 最终日终巡检：`PASS`，`replay_required_count=0`。

## 验证

- `test/test_export_trusted_strategy_candidates.py`
- `test/test_web_task_queue.py`
- 共 59 项测试通过。
