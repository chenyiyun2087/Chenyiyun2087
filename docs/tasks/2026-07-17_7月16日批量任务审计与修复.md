# 2026-07-16 批量任务审计与修复

## 结论

- 7 月 16 日计划任务最终全部完成，日终巡检 `replay_required=0`。
- `sina_score` 首次因当日前复权行情尚未到齐失败，队列第二次自动重试成功；保留原失败历史。
- 蜡烛图扫描原产出 5,187 行，遗漏的 292 只全部为北交所 `920xxx` 股票。

## 根因与修复

- 本地 `ashare_candle_diag` 依赖将 `920xxx` 默认转换为 `.SZ`，已修正为 `.BJ`。
- 日扫脚本改为先完成全量扫描和日期/覆盖校验，再在同一事务中删除旧行并写入新行。
- Web 任务验收改为同时校验全市场总数和北交所行数，不再仅以“存在任意行”判定成功。

## 重跑与验收证据

- 首次安全重扫完成 5,479/5,479，日期门禁识别到停牌股票最后 K 线早于目标日并拒绝写入，原 5,187 行未被删除。
- 门禁调整为“不得使用目标日之后数据，且全市场最新日期必须等于目标日”后再次重扫。
- 最终结果：扫描 5,479 只，失败 0 只，写入 5,479 行，其中北交所 292 行；统一写入时间为 `2026-07-17 08:55:47`。
- 下游保持不变：评分 5,166 行且核心字段无空值，生产候选 5 行，每日信号 4 行。
- 修复后日终巡检 `PASS`，`replay_required=0`；未重跑评分或交易相关下游，未补发历史通知。

## 测试

- 主项目相关测试：45 项通过；日期门禁调整后相关测试：41 项通过。
- `ashare_candle_diag` 全量测试：36 项通过。
