# 2026-08-30 过去一周批量任务审计与数据修复

## 范围

审计窗口为 2026-08-23 至 2026-08-30；SSE 实际交易日为 2026-08-24 至 2026-08-28。历史失败记录保留，不改写队列和 history 审计轨迹。

## 发现

- 原始队列中有 16 条非成功记录：08-24 的旧 release/依赖缺失及其阻塞链，08-26 的晚到行情验证失败及阻塞链，08-28 的符号链接清理失败。
- 08-24 的 `candle_diag_scan` 因运行时缺少 `ashare_candle_diag` 未生成数据，导致 `score_rank_daily` 到蜡烛诊断的关联缺 5178 条。
- 历史重放入口只传递目标日期，但诊断依赖默认读取最新 K 线，第一次重放因此观察到 08-25/27/28 的未来数据；fail-closed 校验拒绝写入，未造成错日污染。
- 08-24 没有完整的日审计记录；08-28 审计明细仍保留旧 cleanup 失败状态。

## 修复与执行

- 扩大数据就绪错误的可重试识别范围：晚到 bar、同日快照未完成、禁止 stale-date 替代等错误进入 `DATA_READINESS` 重试（5 分钟间隔、最多 24 次）。
- `run_candle_diag_daily_scan.py` 在历史重放时对每只股票的 K 线执行目标日截断，禁止使用未来 bar。
- 周图片清理改为定位持久化 source tree，并安全处理 release 目录共享的符号链接。
- 周重放脚本改为动态日期范围、纳入周清理，并按整日审计明细判断是否需要补跑；数据库凭据只从运行时环境接收。
- 发布并启用 release `chenyiyun-prod-20260830-batch-repair3`（commit `3e12afb3`），worker 已重启并处于 `IDLE`。
- 任务队列执行：4404 cleanup 成功；4406 目标日扫描成功；4407 补齐 08-24 审计；4408 补跑 08-28 审计。
- 按原周清理任务设计，删除 2026-08-17 至 2026-08-21 的 5 个图片目录，共 2,175 个图片文件；未删除数据库历史记录。

## 修复后数据校验

| 数据集 | 08-24 | 08-25 | 08-26 | 08-27 | 08-28 |
|---|---:|---:|---:|---:|---:|
| `score_rank_daily` 行数 | 5178 | 5178 | 5179 | 5180 | 5178 |
| `bs_detection_results/ml_detect_v3` | 5545 | 5545 | 5547 | 5547 | 5546 |
| `ads_candle_diag_daily` | 5543 | 5543 | 5543 | 5543 | 5543 |
| score→candle 缺失 | 0 | 0 | 0 | 0 | 0 |
| 行情 bar 行数 | 5545 | 5545 | 5547 | 5547 | 5546 |
| 日审计行数 / 异常行数 | 21 / 0 | 21 / 0 | 21 / 0 | 21 / 0 | 21 / 0 |

附加校验：评分字段、B/S 信号、蜡烛诊断 close、行情 OHLC 均无空值或坏值；策略候选每日 5 条且 key 唯一；选股信号每日 key 唯一；修复后每个任务和业务日的最新队列状态均为 `SUCCESS`。

## 验证命令

```bash
.venv/bin/python -m pytest -q \
  test/test_batch_reliability.py \
  test/test_candle_diag_daily_scan.py \
  test/test_candle_diag_verifier.py \
  test/test_cleanup_sina_bs_detection_images.py \
  test/test_release_runtime.py \
  test/test_web_task_queue.py
```

结果：79 passed。
