# 系统稳定性审计与升级冻结

## 结论

- 生产升级、策略 release 激活、晋级审批和资本扩张已冻结。
- 当前批处理 worker 保持运行；只允许显式标记的稳定性 hotfix 通过发布门禁。
- 代码、队列恢复和跨日业务日绑定完成稳定性修复。
- 普通升级入口保持硬阻断；本次仅发布了两次稳定性 hotfix，未引入功能升级。

## 修复

1. 新增 `config/release_freeze/production_stability_hold.json`，并由发布脚本和策略 release 激活入口统一校验。
2. sell-precommit 优先读取 worker 持久化的 `CHENYIYUN_TASK_BUSINESS_DATE`，避免跨午夜重跑错绑业务日。
3. 月度 BS verifier 先验证已有本月完成产物，再判断合法计划跳过，避免跳过状态覆盖真实结果。
4. 队列 worker 定期回收过期 RUNNING 作业；NULL heartbeat 也按失效处理，不再依赖人工打开管理页。
5. 启动时自动关闭不在权威 pipeline 中的孤立调度行；已修复 `sina_bs` 的假逾期状态，历史记录保留。

## 验证

- 全量 pytest：`2067 passed, 15 skipped, 39 warnings`。
- 生产代码 compileall：通过。
- diff 空白检查：通过。
- 凭据扫描、冻结清单、冻结策略定义、runtime preflight、关键模块 import smoke：通过。
- 数据库季度集成 smoke：通过。

## 运行态

- 当前生产 release：`chenyiyun-prod-20260903-stability-hotfix-2`（commit `7d044b18`）。
- worker launchd：运行中（PID 7586），队列 runner 当前为 `IDLE`，RUNNING 队列 0 条。
- 调度数据修复后，逾期调度 0 条；`sina_bs` 已禁用调度且 `next_run=NULL`。
- 2026-09-02 日终批处理：20 个 `OK`、1 个合法 `SKIPPED_SCHEDULE`，`replay_required=0`。
- 2026-09-03 检查时尚未到首个 09:25 调度时间，暂无今日批处理记录，不能提前判定今日批次结果。

## 解除冻结

需人工复核本审计结论，确认稳定性窗口结束后，明确移除或修改 stability hold 文件；研究、回测和只读审计不受影响。
