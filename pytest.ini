[pytest]
testpaths = test backtest/tests
pythonpath = . backtest/src
markers =
    slow: long-running regression or research validation
    integration: requires a database or external runtime fixture
