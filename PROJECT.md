# 项目目标、研究治理与生产准入

## 1. 唯一核心目标

> 建立能够长期运行、稳定产生净收益，并将风险控制在可承受范围内的量化投资系统。

项目不以单次回测最高收益、最陡净值曲线或最优历史参数为目标。所有策略、风控模块、执行优化和组合分配都必须服务于：

1. 长期稳定的净收益能力；
2. 可理解、可预期、可承受的风险暴露与回撤；
3. 在真实交易约束下可复现、可审计、可运行。

## 2. 决策优先级

```text
第一优先级：本金安全与最大回撤控制
第二优先级：风险调整后收益
第三优先级：长期净收益
第四优先级：原始累计收益
```

任何优化不得为了提高历史收益而突破风险约束，或以复杂性替代可验证的经济逻辑。

## 3. 稳定收益的定义

稳定收益不等于每年盈利，也不等于短期高收益。策略必须同时具备：

- 跨年份、跨市场状态具有正期望；
- 收益来源清晰、可解释；
- 在交易成本、滑点、涨跌停、停牌、整手及成交约束下仍成立；
- 不依赖单一股票、单一事件、单一市场阶段或事后调参；
- 可通过样本外、Walk-Forward、影子账本和真实执行记录复现。

项目优先追求：

```text
较高的长期风险调整后收益
+ 较低的尾部风险
+ 较短的回撤持续时间
```

## 4. 当前策略治理状态

```text
核心选股策略：继续验证长期 Alpha
B2_SCALE40：PRIMARY_RISK_BUDGET_CANDIDATE
FSC40_ENTRY：REJECTED
EPC-1（停买）：REJECTED
T3：RESEARCH_ARCHIVED
Meta Allocator：FROZEN
```

B2_SCALE40 的定义：风险状态下将目标仓位由 60% 降至 40%，并通过现有正常调仓逻辑执行；它是风险预算候选，不是收益增强器。

## 5. 研究优先级

```text
第一层：核心 Alpha
第二层：执行质量
第三层：风险预算
第四层：低相关策略家族
第五层：组合分配与 Meta 层
```

不得跳过前置层级直接研究复杂状态机、Meta Allocator 或高频参数优化。

## 6. B2_SCALE40 当前冻结规则

```yaml
strategy: B2_SCALE40
status: PRIMARY_RISK_BUDGET_CANDIDATE
normal_target_exposure: 0.60
risk_target_exposure: 0.40
execution: T+1 open
parameter_optimization: forbidden
```

以下规则在完成样本外验证前不得改变：

```text
风险状态规则
常态仓位 60%
风险仓位 40%
候选池、Top5、持有期、M7
T 日信号、T+1 开盘执行
交易成本、滑点、整手与成交规则
```

## 7. 研究准入门槛

任何策略或模块进入下一阶段前，必须回答：

```text
收益从哪里来？
最差情况下会亏多少？
```

B2_SCALE40 进入影子运行前，必须同时满足：

1. 相对真实等实际暴露静态基线，Calmar 至少高 0.10，且 MaxDD、CVaR95、Ulcer 不更差；
2. 至少完成 3 个有效 Walk-Forward 验证窗口，且多数窗口中风险指标优于 S60；
3. 压力成本与可成交约束下不劣于等实际暴露静态基线；
4. 不存在未来函数、账本不一致、状态错位、订单回退、理想化成交或不可解释的执行路径；
5. 相对收益和风险改善不由单一风险事件或单一股票主导；
6. 所有规则、代码 SHA、数据快照和配置均可复现。

任一条件失败：

```text
策略状态 = RESEARCH_REPLICATION_REQUIRED
```

## 8. 影子运行与生产准入

### SHADOW_READY

进入无资金影子运行前必须完成：

```text
等实际暴露静态基线验证
Walk-Forward 验证
成本与成交压力测试
事件级归因
代码、配置、数据版本冻结
```

影子运行同时维护：

```text
S60_SHADOW
B2_SCALE40_SHADOW
STATIC_EQUAL_EXPOSURE_SHADOW
```

影子期要求：

```text
至少 60 个交易日
至少经历一次 NORMAL → RISK → NORMAL
每日风险状态、目标仓位、实际仓位、订单、现金、持仓和净值可对账
禁止修改策略参数和风险阈值
```

### LIMITED_LIVE_READY

只有影子运行通过后，才允许小资金生产运行。

```text
首阶段：计划资金的 10%–20%
第二阶段：完成完整风险周期后，提升至 30%–50%
全额部署：仅在前述阶段均符合风险、执行和对账标准后讨论
```

小资金生产的目标是验证执行、成本和风险控制，不是验证短期收益率。

## 9. 停机与降风险规则

```text
数据源异常：停止新增订单
风险状态无法生成：降至保守仓位并人工复核
订单、现金、持仓、净值账本不一致：停止交易并完成对账
实际滑点或成本持续超过压力场景：停止扩容并复核容量
策略回撤达到 12%：预警与归因检查
策略回撤达到 18%：降低策略资金规模，暂停扩容
策略回撤达到 25%：停止策略，进入正式复盘
```

## 10. 禁止偏离事项

```text
为提高历史收益反复微调阈值
根据完整样本挑选最优参数
因单次回测表现优秀而提前升级策略
将复杂状态机当作收益来源
在核心 Alpha 未验证前恢复 Meta Allocator
为了保留某一策略而降低研究标准
将执行路径偶然收益误认为通用风险控制能力
用回测曲线美观程度替代样本外与真实执行证据
```

## 11. 最终成功标准

项目成功不以某段历史回测收益衡量，而以以下能力衡量：

```text
核心策略具备长期正期望
风险预算明确且可执行
最大回撤处于资金承受范围
交易成本与容量约束下仍可运行
多个收益来源之间具备低相关性
策略在影子与实盘环境中能够复现
```

最终追求的是：

> 一个不依赖极端集中押注、不依赖持续人工调参、能够长期积累资本，且回撤可承受、执行可落地的投资系统。
