strategy:
  strategy_id: trusted_champion_rotation_v1
  version: "1.1"
  lifecycle_status: RESEARCH
  lane: SHADOW_DISABLED_RESEARCH_ONLY
  production_mutation_enabled: false
  order_generation_enabled: false

strategy_pool:
  - strategy_id: baseline_full_liquidity_detail_vol_position
    role: liquidity_quality
    allowed_regimes: [BROAD_TREND, NARROW_MOMENTUM, ROTATION, NO_EDGE, RISK_OFF]
  - strategy_id: baseline_full_liquidity_detail_market_gate
    role: balanced
    allowed_regimes: [BROAD_TREND, NARROW_MOMENTUM, ROTATION, NO_EDGE, RISK_OFF]
  - strategy_id: baseline_full_liquidity
    role: defensive
    allowed_regimes: [BROAD_TREND, NARROW_MOMENTUM, ROTATION, NO_EDGE, RISK_OFF, FREEZE]
  - strategy_id: tiered_liquidity_then_bs_v2
    role: attack
    allowed_regimes: [BROAD_TREND, NARROW_MOMENTUM]

ranking:
  primary_window_days: 63
  confirmation_window_days: 126
  confirmation_return_min: 0.0
  confirmation_max_drawdown: -0.20
  confirmation_calmar_percentile: 0.50
  normal_advantage_margin: 0.03
  earnings_advantage_margin: 0.05
  normal_confirmation_days: 3
  earnings_confirmation_days: 5
  minimum_hold_days: 5
  tie_tolerance: 0.0001

v1_1_guards:
  frozen: true
  negative_63_return_blocks_new_risk: true
  reduce_exposure_drawdown: -0.15
  exit_drawdown: -0.20
  reduced_exposure_ratio: 0.50
  estimated_round_trip_impact: 0.0030
  cost_safety_buffer: 0.0050
  low_regime_confidence_threshold: 0.60
  low_confidence_extra_margin: 0.02
  regime_flip_lookback_days: 5
  regime_flip_threshold: 3
  regime_cooldown_days: 5

earnings_season:
  lookback_trade_days: 5
  announcement_density_threshold: 0.10
  fail_closed_on_missing_data: true

portfolio_risk:
  max_single_position_weight: 0.18
  max_industry_weight: 0.35
  switch_notional_ratio: 0.70

execution:
  execution_mode: strict_t1_open_precommit
  base_cost_rate: 0.00075
  base_slippage_bps: 10
  stress_cost_rate: 0.0030
  stress_slippage_bps: 50
  switch_only_on_last_trade_day_of_week: true

walk_forward:
  train_months: 12
  validation_months: 3
  test_months: 3
  step_months: 3
  purge_trading_days: 10
  embargo_trading_days: 5

acceptance:
  min_oos_annualized_return: 0.20
  max_oos_drawdown_abs: 0.20
  min_positive_quarter_ratio: 0.55
  min_calmar: 0.25
  min_calmar_improvement_pct: 10.0
  max_turnover_ratio: 1.20
  min_dsr_confidence: 0.95
  max_pbo: 0.20
  min_stress_annualized_return: 0.0
  required_corporate_action_coverage: 1.0
  max_t_plus_one_violations: 0
  max_order_conservation_errors: 0
  max_single_quarter_profit_contribution: 0.35
  max_single_strategy_profit_contribution: 0.60
  min_switches_for_attribution: 5
  disabled_shadow_days: 20
  disabled_shadow_min_switches: 2
  max_incremental_hard_block_days: 0

promotion:
  stages: [RESEARCH_BACKTEST, SHADOW_DISABLED, SHADOW_ENABLED, CANARY_10, CANARY_25, CANARY_50, CANARY_100]
  enabled_shadow_days: 60
  enabled_shadow_min_round_trips: 30
  canary_days_per_stage: 60
  canary_min_round_trips_per_stage: 30
  canary_capital_ratios:
    CANARY_10: 0.10
    CANARY_25: 0.25
    CANARY_50: 0.50
    CANARY_100: 1.00
  manual_approval_required: true
  broker_api_enabled: false
