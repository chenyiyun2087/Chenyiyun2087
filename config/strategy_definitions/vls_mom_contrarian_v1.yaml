# Formal Strategy Definition: Momentum Contrarian + Value/Size
# v5.2 alpha sleeve — momentum IC=-0.16 (reversal regime 2022-24)
# Hypothesis: A-share short-term reversal → contrarian momentum adds alpha
# when blended with positive-IC value/size/liquidity.
#
# NOTE (2026-08-03 P0 freeze): this file is the SCORING ANCESTOR.  The
# canonical frozen governance identity is vls_mom_contrarian_v1_frozen.yaml
# (identical factor weights/signs; execution params locked at TopN=10,
# hold_days=20, buffer=0.10, band=0.0).  The b=0.05+band0.5 high-return
# variant is vls_mom_contrarian_v1_b005_band050.yaml.  No further parameter
# optimization on the 2022-2024 window is permitted.

schema_version: "strategy_definition_v1"
strategy_id: "vls_mom_contrarian_v1"
strategy_display_name: "VLS + Momentum Contrarian v1"

score_formula: "weighted_sum"
factor_weights:
  value: 0.30
  size: 0.25
  liquidity: 0.25
  momentum: 0.20

factor_signs:
  value: 1
  size: 1
  liquidity: 1
  momentum: -1

missing_value_policy: "BLOCK_IF_EXCEEDS_THRESHOLD"
missing_factor_threshold_pct: 5.0

ranking_policy: "descending"
selection_threshold: "top_20_pct"

rebalance_policy: "weekly_monday"

signal_time: "T15:30:00+08:00"
execution_time: "T+1 09:30:00+08:00"
timezone: "Asia/Shanghai"

code_entry: "scripts.research.build_formal_scores.build_formal_scores"
code_path: "scripts/research/build_formal_scores.py"

acceptance_profile: "alpha_v3_2"
